import { styled } from '@mui/material/styles';
import SmartBox from '../../atoms/box/SmartBox';

export const ActionsWrapper = styled(SmartBox)({
	display: 'flex',
	alignItems: 'center',
	gap: '8px',
});

